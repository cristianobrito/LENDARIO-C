/*
           RIO DE JANEIRO 11 DE MARÇO DE 2023
    TODOS OS EXERCICIOS ENCONTRADOS POR MIM AO LONGO DA
      VIDA ESTARAO AQUI COMO UM ACERVO GIGANTE PARA
                     LINGUAGEM C

                  SEJAM BEM VINDOS
                     CAVALEIROS
                        JEDI

                      by:@NANO
                  britonano@gmail.com

*/
#include <stdio.h>      // NECESSARIOS PARA AS FUNÇÕES FUNCIONAREM
#include <stdlib.h>     // ARQUIVOS DA LIB PADRÃO USAM <> PARA IMPORTAR
#include <stdbool.h>    // PARA USAR O TIPO BOOL (BOOLEANO)
int main()
{
    configuracao();     // CONFIGURACOES DA JANELA
    EX00_0();           // OLA MUNDO
    EX00_1();           // PUTS
    EX00_2();           // CONTA SIMPLES
    EX00_3();           // ENTENDENDO A FUNÇÃO PUTS
    EX00_4();           // EXIBINDO VALORES INT E FLOAT E FORMATANDO CARACTERES ESPECIAIS
    EX00_5();           // DEFININDO E USANDO UMA CONSTANTE
    EX00_6();           // VARIAVEIS DECLARACAO E USO
    EX00_7();           // TIPO BOOL
    EX00_8();           // MODIFICADORES DE TIPOS DE DADOS
    EX00_9();           // USANDO MAIS DE UMA VEZ VARIAVEIS
    EX00_10();          // MISTURANDO VAIAVEIS
    EX00_11();          // LENDO CARACTERES GETCHAR
    EX00_12();          // USANDO PUTCHAR
    EX00_13();          // TRABALHANDO COM CARACTERES
    EX00_14();          // COLOCANDO STRING EM ARRAY CHAR
    EX00_15();          // USANDO SCANF();
    EX00_16();          // SCANF(); COM ENDERECO DE MEMORIA &
    EX00_17();          // LENDO STRING COM FGETS();
    EX00_18();          // CONDICIONAIS
    EX00_19();          // NUMERO SECRETO COM IF
    EX00_20();          // IF ELSE IF ELSE
    EX00_21();          // SWITCH CASE
    EX00_22();          // SWITCH CASE SEM BREAK
    EX00_23();          // OPERADOR TERNARIO
    EX00_24();          // LOOP FOR
    EX00_25();          // FOR ANINHADO
    EX00_26();          // WHILE
    EX00_27();          // DO WHILE
    EX00_28();          // FUNCOES
    EX00_29();          // ARRAYS BASICO
    EX00_30();          // ARRAYS
    EX00_31();
    return 0;
}
